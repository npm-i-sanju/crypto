// WebSocket middleware for real-time price updates
// This is a placeholder - implement when you need real-time features

export const websocketMiddleware = (store) => (next) => (action) => {
  // Handle WebSocket connections here
  return next(action);
};

export default websocketMiddleware;